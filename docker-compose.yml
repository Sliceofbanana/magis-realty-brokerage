services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: magis
      POSTGRES_PASSWORD: magis_dev_password
      POSTGRES_DB: magis_dev
    ports:
      - "5432:5432"
    volumes:
      - magis_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U magis -d magis_dev"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  magis_pg_data:
